import { styled } from "@mui/material";

export const AppContainer = styled('div')`
    display: flex;
    flex-direction: column;
    justify-content: space-between; // or margin: auto; in footer
    min-height: 100vh;
`;